# Session — 2026-04-01

**Machine:** Mac.attlocal.net

## What Was Done
- Fixed playlist `create-from` command — was crashing on first API error (no try/catch around `addToLibrary`)
- Fixed `addToLibrary` 400 errors — `ids[songs]` brackets in URL were not percent-encoded, causing Apple Music API to reject requests
- Fixed fuzzy track mismatches in AppleScript — all playlist commands (`PlaylistAdd`, `PlaylistCreateFrom`, `PlaylistTemp`) now try exact match (`name is "X"`) before falling back to `contains`
- Added quote escaping for track titles/artists in AppleScript strings (prevents injection from titles with quotes)
- Increased library sync wait times from 2s/3s to 4s across all playlist write commands
- Updated skill `SKILL.md` workflow section — now instructs Claude to use batch `create-from` and `&&` chaining instead of individual `playlist add` calls per track
- Saved memory for plugin update command: `claude plugin update music@apple-music-marketplace`

## Key Decisions
- AppleScript matching strategy: exact first (`is`), then `contains` fallback — prevents wrong-track matches while still finding tracks with slight name variations
- Skill guidance explicitly forbids looping `playlist add` — forces `create-from` for batch operations to minimize tool calls on screen

## Commits
- `76b1146` fix: playlist resilience — URL encoding, exact matching, batch error recovery

## What's Next
- Test the full original request end-to-end: "find house tracks, make a playlist, shuffle on Kitchen at 60%"
- Bump version to 1.2.0 and publish to marketplace
- End-to-end testing of all slash commands

---

# Session 2 — 2026-04-01

**Machine:** Mac.attlocal.net

## What Was Done
- Designed CLI UX redesign from scratch via brainstorming: smart positional shortcuts, interactive TUI, result caching
- Wrote spec (`docs/superpowers/specs/2026-04-01-cli-ux-redesign.md`) with 6 sections, 2 rounds of review fixes (4 findings each round)
- Wrote implementation plan (`docs/superpowers/plans/2026-04-01-cli-ux-redesign.md`) with 15 tasks, 3 rounds of review fixes
- Implemented all 15 tasks via subagent-driven development (sonnet-class models, parallel where possible)
- New modules created:
  - `Sources/Models/ResultCache.swift` — domain-specific song/speaker caches with index lookup
  - `Sources/TUI/Terminal.swift` — raw mode, ANSI codes, KeyPress, signal handling
  - `Sources/TUI/MultiSelectList.swift` — multi-select with action keys (play/add/create)
  - `Sources/TUI/ListPicker.swift` — single-select navigable list
  - `Sources/TUI/VolumeMixer.swift` — per-speaker volume bars with left/right adjust
  - `Sources/Commands/RemoveCommand.swift` — new top-level remove command
  - `Tests/MusicTests/ResultCacheTests.swift` — 6 tests
  - `Tests/MusicTests/SmartParserTests.swift` — 9 tests
- Commands rewritten: Speaker (smart parser + TUI + hidden aliases), Play (index + shuffle keyword), Add (index + --to flag), Volume (TUI + name resolution fix)
- Cache writing added to Search, Similar, Suggest, NewReleases
- Interactive TUI added to Similar, Suggest (bare invocation = TUI)
- Playlist updated: index-based create/add, interactive browse via PlaylistBrowse
- Fixed pre-existing bug: volume command didn't resolve speaker names (added resolveSpeakerName)
- Version bumped to 1.2.0 across all three locations
- Updated README.md, SKILL.md description + body, docs/guide.md, vault files

## Key Decisions
- Category 1 (smart CLI shortcuts) only, not category 2 (NLP parsing). Skill handles complex orchestration.
- Domain-specific cache files (last-songs.json vs last-speakers.json) to prevent cross-domain index collisions
- `isBareInvocation(command:)` checks CommandLine.arguments directly, not default values, to detect bare invocations
- `--to` flag (repeatable) instead of `to`/`and` keywords for playlist add, avoids playlist name ambiguity
- Bare browsing command = interactive TUI. Action commands (play, pause, stop) keep current bare behavior.
- Hidden subcommands with `shouldDisplay: false` for backwards compatibility with skill
- All fallback AppleScript matching includes artist (not just title) to prevent wrong-track bugs

## Commits
- `99cedbf` feat: add Terminal raw mode and ANSI helpers for TUI
- `1f2a4f0` feat: add ResultCache module with domain-specific song/speaker caches
- `7469f80` feat: add MultiSelectList TUI component
- `cb61fe4` feat: add ListPicker and VolumeMixer TUI components
- `64bcfab` feat: add SpeakerParser with smart positional arg interpretation
- `fef4f2a` feat: write search/similar/suggest/new-releases results to songs cache
- `267867f` feat: rewrite speaker command with smart parser, TUI, and hidden aliases
- `7e4d1ee` feat: add index lookup and shuffle keyword to play command
- `2965907` feat: add index lookup and --to playlist flag to add command
- `53f9745` feat: add top-level remove command for playlist track removal
- `c56be83` feat: add interactive TUI to similar and suggest commands
- `d8bd7e4` feat: add index-based playlist add/create and interactive TUI browse
- `9782697` feat: add interactive volume mixer TUI
- `da5b87e` fix: add speaker name resolution to volume command
- `68f3a5d` chore: bump version to 1.2.0 for CLI UX redesign
- `d1498dc` docs: update README, skill, and guide for v1.2.0 CLI UX redesign

## What's Next
- Publish v1.2.0 to marketplace
- Update slash commands to use new positional shortcuts where applicable
- End-to-end testing of all interactive TUI modes (Ctrl-C cleanup especially)
- Consider updating slash commands to pass through smart args directly
