# Session — 2026-03-27

**Machine:** Mac.attlocal.net

## What Was Done
- Enhanced `scripts/statusline.sh` to show track, active speakers, and per-speaker volume (e.g., `▶ Say Goodbye — Beck  ·  Kitchen [60]`)
- Converted `commands/play.md` to `disable-model-invocation: true` with inline bash — matches playlists first, then searches library, enables shuffle for playlists
- Created `commands/vol.md` — set volume on all active speakers (`/music:vol 60`, `up`, `down`)
- Created `commands/shuffle.md` — toggle shuffle on/off
- Rewrote `commands/speaker.md` as `disable-model-invocation: true` with inline bash — fuzzy device matching, add/remove/switch/airpods/list
- Updated README with new command list and updated status line description
- Added "list playlist tracks" example to README and SKILL.md (from real session showing Top 25 Most Played)
- Played music throughout session: Working Vibes, Top 25 Most Played, managed Kitchen speaker

## Key Decisions
- All commands now use `disable-model-invocation: true` to eliminate chat clutter from Bash tool calls
- Used `$ARGUMENTS` template variable and `bash -c` pattern for passing user input to osascript safely
- Speaker command uses bash `grep -i` for fuzzy device name matching instead of AI
- Kept the natural language skill (SKILL.md) intact for complex multi-step requests — slash commands handle the common cases

## Commits
- `e175d13` docs: add playlist track listing example to README and skill
- (pending) feat: convert all commands to disable-model-invocation, add vol/shuffle commands, enhance status line

## What's Next
- Test all new `disable-model-invocation` commands after `/reload-plugins`
- If `$ARGUMENTS` doesn't expand in disable-model-invocation, adjust approach
- Bump version to v0.3.0 once verified
